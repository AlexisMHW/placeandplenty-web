This folder is generated automatically by `tinacms build` (part of the
`npm run build` script) and will be populated with the visual editor's
static assets at deploy time. Nothing here needs to be edited by hand.
